package ch9;

public class 연산 {

	/* 단항 연산자 - 증감, ++, -- */
	// 부호 연산자 - +-
	// 증감 연산자 - ++ -- (변수의 값을 1증가 시키거나(++) 1 감소(--)시키는 연산자)
	// 증감 연산자에서 변수값은 앞에붙나 뒤에붙나 변화되지만 = 부등호 앞에있는숫자는 증감연산자가 앞에붙으면 적용된값 뒤에 붙으면
	// 적용되지않은값으로 계산

	/*
	 * 이항 연산자 boolean 타입을 제외한 모든 기본 타입에 사용가능 결과값 산출할때 overflow조심 ex)- byte의 경우
	 * -128~127 넘어가면 오버플러우됌
	 */

	int v1 = 5;
	int v2 = 2;

}
